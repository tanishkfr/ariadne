# Builder OS V1.1 productization — operations log

**Started:** 2026-08-23  
**Branch:** `codex/v1.1-productization`  
**Baseline / rollback point:** `10004c141aadec8867161cdd69531337cc2979d9`

This pass improves the public `$builderos` journey around the proven V1 core.
It does not grant gates, replace canonical policies, or claim unobserved external
provider behaviour.

## Baseline

- Tracked source was clean at the rollback point.
- Historical untracked validation directories A1, A2, A3, and A3R1 variants
  were preserved.
- Repository-local Git identity was `tanishkfr
  <tanishkfr@users.noreply.github.com>`.
- The canonical suites passed when run sequentially: packet transport 24/24,
  runtime 39/39, installer 5/5, repository checks, integrated self-tests, and
  validation guards 29/29.
- Running independent self-test commands concurrently exposed a shared temporary
  directory collision. The suites themselves remained green; concurrency safety
  is a concrete reliability item for this pass.

## Prioritised backlog

| Priority | Problem | Evidence | Smallest owner |
|---|---|---|---|
| P0 | The runtime can send S4B when `HANDOFF.md` exists even if essential implementation context is incomplete | S4A inference checks existence and provider preflight, not content readiness | `scripts/builderos.py` plus the canonical handoff structure |
| P0 | Returning users see stage codes and a thin status instead of goal, direction, risks, and the next human decision | current `status` output | `scripts/builderos.py` |
| P1 | Same-session progression still requires the skill to enumerate outputs and call two low-level commands | current entry-skill procedure | controller `advance` operation plus skill |
| P1 | Provider blocking states name alternatives but do not recommend the recorded fallback or smallest safe split | provider-preflight output | controller preflight recommendation |
| P1 | The generated external packet still requires a manual provider launch/paste; no local Cursor CLI is available on this machine | local command discovery; official provider docs | adapter and generated launch instructions, not a new bridge |
| P2 | Self-test helpers collide when independent suites run concurrently | reproduced during baseline | existing self-test workspace helpers |
| P3 | Some validation and retrospective documentation still describes pre-V1 status | current docs | existing documentation owners |

## Boundaries

- `ROUTER.md`, `WORKFLOW.md`, gate meanings, authority, modes, and canonical
  design/research/QA/evaluation policy are protected.
- Generated intelligence and handoff checks derive from canonical project files;
  they do not become a second decision system.
- External sign-in, implementation-provider execution, G1/G2/G3/G4/G5, push,
  deploy, and publish remain human or external boundaries.
- No remote push, deployment, credential access, or historical evidence rewrite
  is authorised.

## Work log

### 2026-08-23 — baseline and first-use trace

**Why:** V1 proved packet correctness and recovery, while V1.1 asks whether the
product can explain the project and prevent incomplete external handoffs without
making the operator understand stages.

**Action:** Re-ran the complete baseline, inspected the public entry skill,
controller, stage transport, prompts, adapters, method skills, templates,
workflow, router, validation protocol, recent Test B evidence, and operator
documentation. Confirmed the new task discovers both canonical and installed
Builder OS skill entries. Investigated official Codex skill discovery and Cursor
rules, CLI, MCP, ACP, and Background Agent options; no Cursor CLI executable is
available locally.

**Finding:** The best current cross-provider boundary remains a verified shared
filesystem artifact with one human provider launch. MCP is for tools/data, ACP
or the Background Agents API needs provider authentication and adds coupling,
and no installed local Cursor CLI exists to automate a safe launch today.

**Next:** add derived project intelligence, calm status, automatic routine
progression, and a fail-closed handoff readiness check at the existing runtime
boundary.

### 2026-08-23 — checkpoint 1 candidate: project intelligence and handoff readiness

**Why:** A returning operator could see only a stage code and next command, and
S4B eligibility was based on file existence plus provider preflight. A fresh
implementer also did not receive `PROJECT.md`, while the canonical handoff had no
required copy of the outcome or success criteria.

**Action:** Extended the existing runtime to derive a plain-language briefing
and health model from `PROJECT.md`, `DESIGN.md`, `HANDOFF.md`, `AGENTS.md`, QA,
packet lineage, provider preflight, and append-only project notes. Added the
high-level `advance` operation for routine same-session evidence/progression and
durable notes for rejected directions, risks, lessons, recovery, and evidence
gaps. Added a fail-closed handoff-readiness boundary that checks canonical
sections, verbatim thesis and success criteria, content readiness, implementer
discretion, dependency approvals, critical assets, QA, routing, and return
contract. Provider preflight now recommends the supplied fallback or a bounded
split rather than only reporting failure.

The canonical handoff template and S4A prompt gained three demonstrated missing
inputs: outcome/acceptance criteria, content readiness, and the boundary between
implementer discretion and forbidden reinterpretation. This is a structure fix,
not a design or gate-policy change. The structured return now also emits a
derived JSON record beside the preserved Markdown for deterministic resume.

**Validation:** Python compilation passed. Runtime self-test 51/51, including
positive controls and negative cases for missing handoff sections, placeholders,
success-criteria drift, unapproved dependencies, unresolved critical assets,
provider fallback, automatic routine progression, durable non-blocking evidence
gaps, machine-readable returns, and two-project isolation. Packet transport
24/24, repository check, integrated self-tests, and `git diff --check` passed.

**Next:** make self-tests concurrency-safe, then exercise the revised public
journey on a realistic isolated project and fix only observed friction.

### 2026-08-23 — checkpoint 2 candidate: concurrent validation reliability

**Why:** The baseline parallel run made two otherwise-green suites collide on a
shared fixed temporary directory. That is a deterministic reliability defect for
maintainers and CI, not a product-stage failure.

**Action:** Gave the runtime, packet, and skill-installer self-tests independent
uniquely named workspaces under the existing Windows-compatible validation root.
The first attempt used `tempfile.mkdtemp`; Windows ACLs prevented cleanup. That
specific experiment was removed, its two generated directories were deleted,
and ordinary `Path.mkdir` with UUID names replaced it.

**Validation:** All three changed Python files compile. Sequential runtime
52/52, packet 25/25, and installer 6/6 pass. The same three suites plus the
integrated repository self-test also pass concurrently, reproducing and closing
the original collision. No failed-experiment directory remains.

**Next:** run a realistic isolated `$builderos` journey through the revised
high-level entry and record actual operator interventions and exposed machinery.

### 2026-08-23 — checkpoint 3 candidate: realistic public journey and automatic recovery

**Why:** A realistic project is the best test of whether the public skill hides
transport work while preserving real judgement. It also exercises the controller
with project content that was not written to satisfy its fixtures.

**Action:** Started the isolated `v11-memory-atlas` product-app from an ordinary
brief through `$builderos`. The controller prepared intake and focused research
without asking the operator to locate a canonical file, packet, prompt, hash, or
evidence path. S2 used five dated primary-source lookups, produced a scoped
`RESEARCH.md`, and advanced to design direction. S3 inspected three live references
at desktop and narrow widths, synthesised a type-led direction, resolved every
asset, and then stopped at G1. Builder OS requested exactly one genuine human
judgement and did not grant its own gate.

The journey also exposed one P1 recovery mismatch: the public skill promised
same-stage recovery, but `advance` paused on a partial external implementation
return unless the operator knew the low-level `prepare-next --retry` command.
`advance` now recognises a partial or blocked structured S4B return and prepares
the non-overwriting child continuation itself. A complete return remains the
positive control and continues to isolated S5 instead.

**Validation:** Python compilation passed. Runtime self-test 52/52, including the
automatic partial-return continuation, parent lineage, preserved prior evidence,
machine-readable complete return, and complete-return-to-S5 positive control.
`git diff --check` passed.

**Intervention trace:** human decisions requested: 1 (G1); manual packet assembly:
0; manual canonical-file lookup: 0; manually exposed stage IDs/hashes/paths in
normal controller output: 0; external provider launch: not attempted; fabricated
provider evidence: none.

**Next:** checkpoint the recovery fix, then harden provider return/re-entry
guidance, operator documentation, and the ready-for-validation Cursor fixture.

### 2026-08-23 — checkpoint 4 candidate: non-destructive adoption and complete briefings

**Why:** The public runtime rejected every non-empty project, so an operator with
working code had no supported entry path. The realistic return briefing also
clipped a multi-line Markdown goal after its first source line.

**Action:** Added an explicit `start --adopt-existing` path to the existing
runtime and packet transport. It is opt-in, preserves all existing files and
behaviour, supplies a read-only adoption context to S1, and records adoption in
the packet manifest and run state. Ordinary start still refuses a non-empty
directory. Adoption refuses to overwrite `PROJECT.md` or `AGENTS.md`, directing
the operator to resume or migrate deliberately instead. Updated the entry skill,
Codex adapter, README, getting-started guide, and daily playbook with the same
boundary. Project intelligence now joins the first Markdown paragraph for the
goal, so a fresh-session briefing presents the complete outcome.

**Validation:** Python compilation passed. Packet transport 29/29 and runtime
55/55 pass. Negative controls cover implicit adoption, existing AGENTS/PROJECT
overwrite, malformed adoption provenance, and entry-skill drift. Repository
check, integrated self-tests, and `git diff --check` pass. A real status call on
the paused Memory Atlas project now prints its complete multi-line goal.

**Next:** checkpoint adoption, then finish provider-validation instructions,
V1.1 readiness evidence, stale-status documentation, and final full validation.

### 2026-08-23 — checkpoint 5 candidate: fresh-task re-entry and Cursor-ready transport

**Why:** Controller fixtures prove state transitions, but V1.1 also needs a real
clean-task re-entry and a current external-provider packet without claiming the
provider ran.

**Action:** Created one new read-only Codex task with no prior conversation and
asked it to invoke `$builderos` for the paused Memory Atlas project. It discovered
the single run, reported the full goal, current design-direction work, G1 health
boundary, and next human decision. It reported zero manual prompt, packet,
manifest, hash, transcript, or run-directory lookup and made no project change.

Verified the historical Test B B1 S4B packet first; it correctly failed as stale
because `prompts/build-kickoff.md` changed. Generated a distinct
`B1-S4B-V11-READY` packet from the preserved B1-S4A transcript and current
canonical sources. Verification passes with provider `cursor`, parent `B1-S4A`,
verbatim transcript parent evidence, and exactly the current S4B prompt,
`HANDOFF.md`, locked `DESIGN.md`, `AGENTS.md`, `QA-POLICY.md`, `templates/QA.md`,
and `templates/RETURN-HANDOFF.md`. Earlier reasoning conversation remains
forbidden. Cursor was not opened, so question count and live return behaviour
remain unobserved.

**Evidence:** fresh task `01a02b3d-4a7d-7512-aa6e-2d84205b03ca`; current Cursor
packet SHA-256 `8CD64DCA8295F10DFE73443686D3B6068134AA0CD30F3AD458C3B49AD2DB16B9`;
manifest SHA-256 `7404D0A2973272F190E92A8099F09E00D609F01D8F29BF5383E38A4D3D9B8DBC`;
parent transcript SHA-256 `51508A644DA94178B598B69AC0902ACFB12AD78FF432CC342697E9EF6BC513C6`.

**Next:** publish the repository-local readiness report, run the complete suite,
review protected-file scope, and create the final local checkpoint commit.

### 2026-08-23 — final validation candidate

**Action:** Published `V1.1-READINESS.md`, reconciled operator documentation with
implemented behaviour, and ran the full suite sequentially. Verified the current
Test B Cursor packet again. Checked every protected policy path against baseline
`10004c141aadec8867161cdd69531337cc2979d9`; there is no protected-policy diff.

**Results:** Python compilation passed. `check.py` passed; its integrated
self-test passed. Packet transport passed `29/29`, runtime controller `55/55`,
skill installer `6/6`, and validation guards `29/29`. `git diff --check` passed.
The managed personal skill verification correctly reports source drift because
this candidate has not been installed outside the repository; no installation
was performed without the operator's approval.

**Release boundary:** V1.1 is READY for private real-project use and live Cursor
validation. Live Cursor completion/return, public naming, Test C and the protected
S6 semantics question remain explicitly unproven or human-owned. No dependency,
credential, remote push or deployment was used.

# Builder OS V1.2 creative intelligence operations

**Started:** 2026-08-23  
**Branch:** `codex/v1.2-creative-intelligence`  
**Rollback:** `80561311d41522bf911d28558c577f370c0bd233`  
**Git identity:** `tanishkfr <tanishkfr@users.noreply.github.com>`

This log answers what changed while the operator was away, why it changed, what
was actually verified, and what remains outside the available environment.

## Safety boundary

- `ROUTER.md`, `WORKFLOW.md`, gates, authority, modes, S5 isolation and provider
  neutrality are not being changed.
- `RESEARCH-POLICY.md`, `DESIGN-TASTE.md`, design/QA methodology and existing
  templates remain canonical.
- Existing method skills keep their method. Small evidence-boundary additions
  address the demonstrated defect that recommendation, execution and use were
  not durably distinguishable.
- Historical validation evidence is preserved. No Cursor execution, credential,
  dependency installation, remote push or deployment is authorised or needed.

## Baseline

V1.1 HEAD `80561311d41522bf911d28558c577f370c0bd233` was green before editing:

- Python compilation: pass;
- repository check: pass;
- integrated self-test: pass;
- packet transport: `29/29`;
- runtime controller: `55/55`;
- skill installer: `6/6`;
- validation guards: `29/29`;
- `git diff --check`: pass.

## Prioritised V1.2 backlog

| Priority | Concrete defect | Smallest owner |
|---|---|---|
| P0 | A skill could be described as complete when only recommended | project-local evidence ledger + runtime state machine |
| P0 | DESIGN/RESEARCH source claims had no structural inspection-artifact check | creative evidence validator |
| P1 | Research depth and skill choice were implicit and not durable | structured post-S1 assessment + recommendation engine |
| P1 | A found, inaccessible, inspected and used reference collapsed into prose | reference evidence states + decision trace |
| P1 | Fresh S2/S3 sessions did not receive creative provenance | existing stage packet transport |
| P2 | Generic or cosmetically different directions were human-only catches | pre-G1 deterministic negative controls, without granting G1 |
| P2 | Fresh-task/resume survival was not tested for creative reasoning | isolated ledger reload and stale-artifact controls |

## Checkpoint 1 candidate — evidence state and S2/S3 transport

**Established defect:** V1.1 could prove that a packet and output file existed,
but could not prove the difference between a recommended skill, an invoked
method, a completed output and downstream use. A URL in `DESIGN.md` or
`RESEARCH.md` could also assert its own inspection without an independent
retrieval artifact.

**Change:** Added one project-local `.builderos/creative-evidence.json` schema
owned by `scripts/creative-intelligence.py`. It selects research depth and
project-specific methods from a reasoned assessment, records strict skill-state
transitions, separates found/inspected/inaccessible references, hashes evidence
and output artifacts, records conflicts and reference-to-decision traces, and
checks external claims in `RESEARCH.md` and `DESIGN.md`.

The existing S2/S3 packet generator now carries that ledger when present and
leaves legacy runs explicitly untracked when absent. S5 receives none of it.
The Builder OS entry skill hides assessment/event mechanics from the user and
uses progressive disclosure through one focused reference file.

**Focused validation:**

- creative intelligence: `18/18`;
- packet transport: `29/29`, including positive S2/S3 ledger delivery;
- runtime controller: `56/56`;
- repository check: pass;
- integrated self-test: pass;
- Python compilation: pass.

**Not claimed:** No public web source or external provider was exercised by
these deterministic fixtures. Synthetic source files prove the evidence
contract, not the truth of an external page.

**Next:** checkpoint this coherent group, then run realistic isolated projects
through the public `$builderos` path and fix only observed friction.

## Checkpoint 2 candidate — tamper resistance, resource decisions, realistic evidence

**Established defects:** A hand-edited ledger could omit an invocation event
while retaining a matching final state. Resource comparisons also had no
structured way to retain compatibility, licence, cost, alternatives, necessity,
inspected sources, and their exact downstream decision.

**Change:** Ledger validation now replays each skill history from its expected
initial state. Resource events now record an evaluated library, framework,
browser API, font, icon set, asset/image source, technique, design tool, or
component primitive against at least one inspected source and one named
alternative. The decision must anchor to a hashed downstream artifact. The S2
prompt and Builder OS skill expose this evidence contract without installing or
approving a dependency.

**Realistic reference workflow:** The isolated Living Margins fixture at
`C:\Users\snprasad\Builder OS Tests\v12-living-margins` was run through the
public controller from S1 to S3. Three live pages were actually loaded and
visually or interactively inspected on 2026-08-23:

- `https://publicdomainreview.org/collections/` — visual capture;
- `https://storycorps.org/stories/` — visual capture;
- `https://neal.fun/space-elevator/` — interaction exercised from 0 m to a
  visible 1513 m state, then captured.

The ledger distinguishes found from inspected, hashes the captures, records a
reference conflict, compares a continuous trace with a retrieval-led index,
and traces three decisions into `DESIGN.md`. The controller stopped at the
human G1 boundary; no approval or build was fabricated.

**Minimal/fresh-process workflow:** The isolated Focus Timer fixture at
`C:\Users\snprasad\Builder OS Tests\v12-minimal-focus-timer` chose minimal
research and skipped reference analysis, component research, motion, and asset
research. A separate process discovered the run from only the project path,
verified the S3 packet, reloaded creative state, and correctly reported the
unresolved design-direction method.

**Focused validation:**

- creative intelligence: `22/22`, including history tampering, resource
  comparison, dependency rejection, and uninspected-source controls;
- integrated checker: pass, including a negative-tested S2 resource contract;
- packet transport: `29/29`;
- runtime controller: `56/56`;
- validation guards: `29/29`;
- repository check: pass;
- Python compilation and diff whitespace check: pass.

**Not claimed:** The technical-resource fixtures use synthetic inspected source
artifacts and prove the contract, not current package facts. No external
provider, Cursor session, implementation, G1 approval, push, or deployment ran.

## Final evidence projection and documentation

A live status read of the paused Living Margins fixture exposed two misleading
labels even though the underlying gates were sound. Provider preflight selection
could still appear as “not yet selected” until a return existed, and a draft S3
thesis appeared under `approved_direction` before G1. The status projection now
keeps selected / executed / returned-successfully provider evidence separate,
and exposes a draft thesis as `proposed_direction` while
`approved_direction` remains `not yet approved`. Runtime negative/positive
controls cover both boundaries.

Operator docs and `V1.2-READINESS.md` now describe only implemented behaviour.
The personal installed Builder OS skill was not overwritten; it must be
refreshed deliberately after this branch is accepted.

## Final concurrency finding and validation

The first fully concurrent final run reproduced one remaining V1.1 reliability
defect: while the installer self-test's managed skill fixture existed,
`check.py` scanned it as repository Markdown and reported 80 duplicate
sentences. This was not a canonical duplication. The checker now excludes only
the known ephemeral self-test directory patterns while retaining similarly
named durable validation paths. Positive and negative controls prove both sides.

The complete concurrent rerun passed:

- repository check: pass, 389 links, 0 duplicate sentences;
- integrated self-test: pass;
- creative intelligence: `22/22`;
- runtime controller: `59/59`;
- packet transport: `29/29`;
- entry-skill installer: `6/6`;
- validation guards: `29/29`;
- Python compilation and `git diff --check`: pass.

Protected routing, workflow, mode, gate, canonical policy, template and S5
review-isolation owners have no diff from the V1.1 baseline. Historical
untracked A-series evidence remains present and unstaged.

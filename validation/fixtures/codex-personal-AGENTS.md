# Personal defaults

> Fixture. Copied to `~/.codex/AGENTS.md` by `scripts/setup-test-a.py`.
> **Preferences only — never policies.** Ariadne policy lives in the Ariadne
> repository and reaches a session through the pasted stage prompt, not through here.
> Edit this file, not the script, if you want different defaults.

- Indian English spellings.
- pnpm. Never npm or yarn.
- Ask before installing anything.
- Ask before pushing, deploying, or publishing.
- Never read or write `.env` or any credential.
- Prefer the smallest thing that solves the problem.
- End every response with a concrete next action. Never "let me know how you'd like to proceed".
- If something needed is missing, say so and stop. Do not guess and do not invent a decision.
